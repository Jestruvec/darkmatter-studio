---
import SecondaryLayout from '@/layouts/SecondaryLayout.astro';
import { team } from '@/data';
import { TeamCard } from '@/components';
---

<SecondaryLayout title="Equipo">
  <section
    id="team"
    class="flex items-center py-24 px-6"
    aria-labelledby="team-heading"
  >
    <div class="mx-auto max-w-7xl">
      <h2
        id="team-heading"
        class="text-3xl font-bold text-center text-gray-800 mb-6"
      >
        Nuestro Equipo
      </h2>
      <p class="text-center text-gray-600 max-w-3xl mx-auto mb-12 text-lg">
        Creamos soluciones digitales modernas, escalables y centradas en el
        usuario. Nuestro equipo combina creatividad, tecnología y compromiso con
        la excelencia.
      </p>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {team.map((t) => <TeamCard teamMember={t} />)}
      </div>
    </div>
  </section>
</SecondaryLayout>
